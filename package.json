{
  "name": "agent-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Adapter — listens to local AI coding agents (Claude Code, Codex, Cursor, Gemini CLI, OpenClaw, Hermes) and reacts back (answer/confirm/prompt/interrupt). ACAP client; Commander not included.",
  "bin": {
    "agent-adapter": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js start",
    "dev": "node --watch dist/cli.js start --local",
    "verify": "node dist/cli.js verify",
    "test": "tsc -p tsconfig.test.json && node --test \"dist/__tests__/*.test.js\"",
    "ci": "npm run build && npm run verify && npm test",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=22"
  },
  "optionalDependencies": {
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
